## 바인드 인자(Bind Args)

`.sq` 파일은 [SQLite 바인드 인자(Bind Args)](https://www.sqlite.org/c3ref/bind_blob.html)를 포함하여 SQLite와 정확히 동일한 구문을 사용합니다. 문(statement)에 바인드 인자가 포함되어 있으면, 연관된 메서드에서 그에 상응하는 인자가 필요합니다.

{% include 'common/query_arguments.md' %}